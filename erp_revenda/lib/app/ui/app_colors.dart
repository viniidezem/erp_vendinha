import 'package:flutter/material.dart';

class AppColors {
  // Gradiente de fundo (inspirado nas telas)
  static const gradientStart = Color(0xFF6F8ED6);
  static const gradientEnd = Color(0xFF3F66B3);

  // Primárias
  static const primary = Color(0xFF3F66B3);
  static const primarySoft = Color(0xFF6F8ED6);

  // Superfícies
  static const surface = Color(0xFFFFFFFF);
  static const surfaceAlt = Color(0xFFF5F7FB);

  // Bordas
  static const border = Color(0xFFE6ECF5);

  // Texto
  static const text = Color(0xFF1B1F2A);
  static const textMuted = Color(0xFF6B7280);

  // Feedback
  static const danger = Color(0xFFE5484D);
  static const success = Color(0xFF2FB344);
}
